# Main App

This package contains the main maintenance frontend.

From the workspace root:

```sh
npm run dev:main
```

If you need a non-root deploy base, set `VITE_APP_BASE`. Otherwise the app now defaults to `/`.
